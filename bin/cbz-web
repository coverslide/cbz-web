#!/usr/bin/env node
var http = require('http')
var cbz_web = require('../')
var path = require('path')
var config = require(path.resolve('config.json'))

var server = http.createServer()

cbz_web(config, server)

server.listen(config.socket || config.port, config.host)
